#include <stdio.h>
#include <iostream>
#include <time.h>
#include <stdio.h>
#include "Header.h"
using namespace std;

void main()
{
	/*int arr[20];
	FillIn(arr, 20);
	PrintMatrix(arr, 20);
	printf("\n\n");
	sortArray(arr, 20);
	PrintMatrix(arr, 20);
	printf("\n\n");
	for (int i = 0; i < 20; i++) {
		if (findCouples(arr+i) != NULL)
		{
			printf("%d\t", i);
		}
	}*/
	char arr[60] = "123apr test tesrty qwerty";
	word(arr);
}